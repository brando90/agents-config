# Check remote recovery and synchronize the editor checkout

**TLDR-start:** [vb: remote sync] Check the existing remote jobs and verified main publications, then safely synchronize the VeriBench checkout open in the editor. This schedule observes existing work and performs safe local catch-up; the remote owners retain execution and recovery responsibility.

Each trigger performs one finite pass, with a five-minute total deadline. Before writing a receipt or touching any checkout, acquire a nonblocking coordinator-run lock at /Users/sanmikoyejo-mba-1/.codex/task-state/vb-remote-sync/run.lock, held throughout this invocation; if another owner holds it, skip this invocation without taking over. Bound each external read, network call, ownership negotiation and publication operation to 30 seconds and each lock acquisition to five seconds, with at most one retry for a failed read and no automatic retry for mutations. After a timeout, preserve an explicit unknown/stale observation, perform remaining independent safe checks only within the total deadline, and defer extended repairs to the existing owner. Release locks on exit; never steal a live lock.

On each run, read the current agents-config Trigger Rules 46–48 and the freshest master checkpoint for task 01a08dfa-efbd-71e3-b877-9245ab9c1127 from GitHub main through the owned checkout /private/tmp/vb-reset-recovery-repo-20260911. Verify exclusive ownership before reusing that isolated checkout, using the same no-write-window discipline as the editor checkout; create a new uniquely named owned mirror if it is unavailable or in use; never substitute the shared editor checkout for isolated work. Resolve current experiment paths from main and any accepted rename map, because names can change. Keep a local receipt under /Users/sanmikoyejo-mba-1/.codex/task-state/vb-remote-sync/ with the real timestamp, observed jobs, before/after commits, outstanding blockers, notification fingerprints and schedule identifier.

Check the existing remote supervisor 01a08e0e-8d58-73a2-81bf-7468b328a8e9 and evaluation owner 01a08d56-fcf9-7b33-a1fe-2fbd0a8b2400 on skampere1.stanford.edu, following any documented successors. Read fresh results/checkpoints under /dfs/scratch0/brando9/vb-reset-recovery-20260911, actual process identities and descendants, driver state and monitor heartbeat. Check relevant pull requests and exact merged commits against matching landing records. A live terminal, completed model turn, open pull request or configured schedule is not completed research. Completed proof jobs must not be restarted. Preserve all scientific model/effort pins and required review gates; keep a known quota blocker explicit rather than probing it repeatedly. Avoid sending unchanged queued messages to live owners. If intervention is necessary, report it clearly and coordinate the one existing owner under Rule48; this schedule does not grant duplicate job launches or changes to the experiment.

Do not enter a sleeping/polling loop. Record unavailable observations and defer extended repair or review to the existing task owner. Fetch current main for brando90/veribench and brando90/agents-config using bounded operations, and resolve each fetched main to an immutable commit identifier before checking the incoming diff. The user-designated editor checkout is /Users/sanmikoyejo-mba-1/veribench. Before updating it, verify it is on local main, has no tracked/staged changes or unpublished commits, has no merge/rebase in progress, and has exclusive write ownership for the complete update window. Coordinate a no-write window with active owners and hold the checkout synchronization lock throughout inspection, update and verification; known paused writers must remain paused. A lock without the other writers respecting it does not establish exclusivity. Inspect active tasks/processes with access to that checkout; an idle activity badge alone is insufficient. Check the incoming changed paths against untracked AND ignored files, including parent/child path collisions; never overwrite local-only data. Recheck the original HEAD, branch, status, ownership and local-only paths immediately before an allowed git merge --ff-only --no-autostash --no-overwrite-ignore <verified-main-commit>, using the exact commit just checked, not a movable remote-tracking branch name. Verify the same exclusive window remained in force after the merge. Do not use stash, reset, clean, rebase, forced checkout or branch switching in the shared checkout. If ownership/activity or file safety is uncertain, stop its update and synchronize only the owned isolated checkout, explicitly reporting that Cursor's checkout remains behind. Preserve unrelated local changes in agents-config; use an isolated mirror for it if any changes exist. No pull request is needed to pull already-merged changes.

After successful local sync, verify HEAD against the fetched main revision, recheck status and preserve any prior untracked/ignored data. Detect duplicate numeric experiment prefixes against the accepted main layout. Do not automatically delete or move a duplicate: compare it with main and preserved archives, identify active dependencies/writers, and report unresolved leftovers; this run's safe fast-forward is not permission to discard local work. Keep policy changes or repairs requiring review outside routine polling.

Stay quiet while state is unchanged or non-actionable. Notify here only on meaningful new results, verified completion, a changed blocker, or a local-sync failure, with the actual freshness and checkout commit. Treat an unchanged known blocker as already reported. Update the master checkpoint only for meaningful changes, using its owned checkout and the established remote landing lock; do not create progress commits on every poll. Route meaningful Astra evaluation completion or changed-blocker observations to its original local owner task 01a08d3d-11ce-7602-81e7-5643055ee9e8 as well as this recovery task; deduplicate using the saved observation fingerprint. Check the worker completion-email receipt at actual completion. Existing worker completion emails remain their responsibility; do not resend them or alter the manually paused per-job schedules.

Stop this schedule when every owned job has a verified completed or explicitly canceled disposition, required publications are confirmed, and the designated editor checkout has caught up, or when the user cancels. A blocked unfinished job does not meet the completion condition. The local computer and Codex app must be available for this schedule; remote execution and failure reporting must remain independent.

**TLDR-end:** [vb: remote sync] Inspect existing remote work and its Git evidence, safely fast-forward a quiescent local main checkout, and report meaningful changes to this master conversation and the named original evaluation task. Preserve local edits, scientific requirements and single-owner execution; stop after verified completion and local catch-up.
