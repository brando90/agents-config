# Mega QA (quality assurance) 2026-09-08 — strand A, stage 1

**TLDR-start:** [agents-config: quality assurance stage 1] Seventeen corrective commits repair the reviewed scripts and rule contradictions. The four test suites pass all 53 tests; zero critical or major findings remain within the reviewed scope.

Created: 2026-09-09 12:20 PDT
Last updated: 2026-09-09 12:21 PDT
Reviewer: Codex `gpt-6-astra`, reasoning `ultra`.
Scope: the 19 commits `3fcaa86^..ab715a0`, plus the explicitly requested related settings and workflow documents.
Working branch: `megaqa-2026-09-08/A`; starting commit: `ab715a0`; final corrective commit: `cd37613`.
This is stage 1 only. Stages 2 and 3 were not run.

```text
VERDICT: FIXED
CRITICAL_ISSUES: 0
MAJOR_ISSUES: 0
FIXES_APPLIED: 17
STRUCTURAL: IMPROVED
SUMMARY: Seventeen corrective commits pass deterministic checks and independent review within stage 1. Counts above describe remaining in-scope blockers; platform and checkout limitations are recorded below.
```

The review read the requested commit history and patches, complete affected scripts, rules, settings, and related documents. Bounded independent reviewers covered the board, dispatch scripts, and slide synchronization; reviewers also cross-checked the shared-rule and workflow fixes. No changes were pushed or merged, and the shared `~/agents-config` and `~/veribench` working files were not edited. The shared configuration was refreshed and read at task start. Staged diffs and secret scans passed before each commit.

**Findings and corrective commits.** Line numbers below refer to the corrected files; quoted fragments identify the relevant original defect or retained contract. Severity describes the defect before correction.

1. **MAJOR — `scripts/agent_board.py:274`, “unless ps answered.”** The process-table guard from `8da03bf` still accepted nonempty partial standard output from a failed `ps` command. That could identify live sessions as dead and duplicate them. Commit `f4738cd` rejects any nonzero process-scan status before parsing, with a regression that verifies no terminal command is issued. `CUDA_VISIBLE_DEVICES='' python3 scripts/test_agent_board_resume.py` exited **0**, 19 tests.

2. **MAJOR — `INDEX_RULES.md:74`, `claude-code-settings.json:35`, “gpt-6-astra … xhigh” / “claude-fable-5[1m]”.** Trigger Rule 36 and settings/setup instructions had stale effort or model defaults. Commit `44f49b0` pins review defaults to `gpt-6-astra`/`ultra` and `claude-fable-5-1`/`max`, including the root settings fields and setup writer; workload examples retain the regular tier. Structured settings assertions, exact model searches, document path checks, and `git diff --check` exited **0**. Remaining `xhigh` occurrences are supported option values or comparisons explaining `ultra`, not stale review defaults.

3. **CRITICAL — `machine/mac.md:46`, “Mistral provider keys and a direct Python client.”** The setup instructions supplied a direct provider-client path that contradicted Hard Rule 9. Commit `9bdb8a1` replaces the executable advice with a historical explanation and the approved authenticated command-line interface (CLI) workflow. No provider call was run. Independent review and `git diff --check` passed, exit **0**.

4. **MAJOR — `INDEX_RULES.md:130`, “--match-head-commit”, “LANDED”, and “--delete-branch”.** The landing protocol confused a verified worker commit with a verified current destination, treated queue acceptance as completion, relied on a local hook that `gh pr merge` does not run, searched commit messages for file content, and left record publication recursively ambiguous. Commit `87becde` requires enforced verification against current `main`, confirms actual `MERGED` state, reads the checkpoint on `origin/main`, matches the exact pull request (PR) and merge commit, makes record-only follow-ups finite, and separates branch cleanup from merging in an active worktree. It also reconciles standing email authorization and failure subjects and exports the Claude token variable. A temporary Git reproduction of the checkpoint watch and `git diff --check` exited **0**; installed Git/GitHub command help was checked. The [GitHub merge command documentation](https://cli.github.com/manual/gh_pr_merge) and [protected-branch documentation](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches) support the head-match and current-base distinction.

5. **CRITICAL — `scripts/sync_slide_decks.py:494`, “successfully produced”.** Both renderers could accept a generated portable document format (PDF) file even when the converter exited nonzero; timeout/operating-system failures could leave old manifest evidence usable. Commit `2b31bbd` rejects failed render results and invalidates the derivative record. The staged script/test snapshot passed **9 tests, exit 0**; the final `CUDA_VISIBLE_DEVICES='' python3 scripts/test_sync_slide_decks.py` passed **17 tests, exit 0**.

6. **CRITICAL — `scripts/sync_slide_decks.py:685`, “--diff-filter=ACMRD”.** Staged type changes and unmerged paths were omitted, and Git read failures could become empty state and therefore a passing check. Commit `33ac606` includes those statuses and fails closed on unreadable index/blob state. The staged snapshot passed **12 tests, exit 0**; the final slide suite passed **17 tests, exit 0**.

7. **MAJOR — `scripts/sync_slide_decks.py:861`, “if not decks”.** A working-tree check returned before inspecting a stale manifest when the last deck disappeared. Commit `e186cc6` keeps manifest validation active in this case. The staged snapshot passed **13 tests, exit 0**; the final slide suite passed **17 tests, exit 0**.

8. **MAJOR — `scripts/sync_slide_decks.py:638`, “already wired”.** A hook marker after an early exit could be mistaken for a reachable freshness check; a managed hook could also lose its executable mode. Commit `f9ab960` recognizes only the exact managed hook body, restores its mode, and requires manual integration for custom hooks. The staged snapshot passed **15 tests, exit 0**; the final slide suite passed **17 tests, exit 0**.

9. **MAJOR — `scripts/sync_slide_decks.py:138`, “.key”.** Directory-format Keynote packages were omitted from discovery and staged inventory, allowing them to bypass promised freshness checks. Commit `e440fb1` detects and rejects package directories with an instruction to export a single-file deck. Trigger Rule 40 now states the same limitation. The staged and final slide suites each passed **17 tests, exit 0**.

10. **MAJOR — `scripts/deploy_cc.sh:135`, “if marker in c” / “os.kill(pid, 0)” / “<cmd>”.** Readiness could accept an unrelated process mentioning the runbook, a zombie, or a recycled registry process identifier. The preview was schematic and the Codex effort quoting lost the intended configuration string. Commit `2c90dfb` verifies agent executable, ancestry, non-zombie state, start time, registry/session identity, and stable identity across two observations; prints replayable exact arguments; pins Codex settings/permissions; handles decimal `--wait 08`; and preserves failure diagnostics. `bash -n scripts/deploy_cc.sh` exited **0**. Real temporary-runbook previews for all three profiles exited **0**. The exact user-suggested `/dev/null` preview exited **2**, because the documented runbook must be a regular file. The initial dispatch suite passed **6 tests, exit 0**; the final suite passed **12 tests, exit 0**.

11. **MAJOR — `scripts/snap_dispatch.sh:54`, “run <job-name> <command...>”.** Argument boundaries, exact terminal targets, concurrent duplicate refusal, account-specific log paths, explicit `exit` status recording, and very fast job completion needed correction. Commit `6b408e1` validates names/account, uses exact targets, preserves shell-program versus argument-vector input, locks the remote launch before writing files, creates unique runners, publishes the latest-log link only after launch, and records the job status in a subshell. `bash -n scripts/snap_dispatch.sh` exited **0**; the final dispatch suite passed **12 tests, exit 0**. Mock remote launch failure exits **43**, duplicate refusal **1**, and successful detached launch **0** even when the job itself exits **7**. An isolated real local terminal server retained pane statuses **0** and **7** for `true` and `exit 7`, with matching log trailers, then was removed. No real Secure Shell (SSH) launch was required.

12. **MAJOR — `scripts/snap_health.sh:427`, “claude-fable-5[1m]”.** The authentication smoke probe used a model outside current Hard Rule 8. Commit `6d6ab79` pins `claude-fable-5-1` with maximum effort. `bash -n scripts/snap_health.sh` exited **0**; the final dispatch suite passed **12 tests, exit 0**, including mock argument verification for current models.

13. **MAJOR — `machine/macos-ai-apps/ai_agent_automatable_setup_codex_clauded.md:102`, “local status”.** Both documented privilege-session helpers assigned zsh's read-only `status` variable, breaking status preservation and cleanup. Commit `543a2e6` uses `task_exit_code` and a guarded command so failures still reach cleanup. Extracted helpers ran under both bash and zsh with mocked `sudo`/`sleep`: success **0**, failure **7**, and credential revocation confirmed; verification harness exit **0**. The optional shell block passed both syntax checks, exit **0**. The reusable setup prompt now has a closing summary.

14. **MAJOR/MINOR — `INDEX_RULES.md:35`, `AGENTS.md:29`, `CLAUDE.md:21`, `workflows/qa-correctness.md:11`, “never bail” / “guaranteed available” / “claude login”.** Fallback instructions contradicted the Hard Rule 8 stop condition, entrypoint mirrors omitted policy details, dispatch guidance assumed an unavailable model flag, and prompts/Rules 41–46 had missing first-use explanations. Commit `fe1a248` reconciles fallback and merge authority, uses `claude auth login`, adds terminal prompt summaries, clarifies naming and model choices, and expands abbreviations and metaphors without a wholesale style rewrite. Independent review, document path checks (**45/45**), shell/prompt checks, and `git diff --check` passed, exit **0**.

15. **MAJOR — `workflows/remote-job-dispatch.md:3`, “same smart-mode agent wrapper” / “codex exec --full-auto”.** The guide recommended legacy wrappers whose local implementations still use stale flags or unpinned models. Commit `f244bfd` documents the verified direct-mode options, explicit agent commands, named-session ownership, actual job lifetime, and the worker's completion responsibilities. The out-of-scope scheduler/launcher implementations were not edited. Local source/help confirms `--direct`, `--default-mode direct`, `--mode direct`, and `JOB_MODE: direct`. Document path checks and `git diff --check` exited **0**.

16. **MAJOR — `experiments/01_self_hosted_openclaw/standing_orders/experiment_dispatch.md:3`, “claude --headless” / “--output-dir”.** The standing order read like an installed command but was a deferred design, named invalid Claude flags, and equated a disappearing terminal session with success. Commit `7077439` explicitly labels its status, uses valid runner commands, defines isolated project worktrees and unique sessions, requires recorded completion, and applies cluster/authentication/landing requirements. Local command help, document path checks, and `git diff --check` exited **0**. No Telegram workflow was implemented or invoked.

17. **MINOR — `scripts/deploy_cc.sh:84`, `scripts/snap_dispatch.sh:183`, `INDEX_RULES.md:132`, “TLDR/Snapshot protocol.” / “claude -p PONG”.** The generated opening prompt still omitted its own terminal summary and the printed/prose probe examples lacked explicit model settings. Commit `cd37613` adds the closing TL;DR (too long; didn't read) summary and pins the examples to `claude --model claude-fable-5-1 --effort max -p PONG`. Both affected scripts passed `bash -n`, the Codex preview exited **0**, document paths passed, and an independent reviewer confirmed the flags and shell quoting. This final change alters prompt/help text only; the behavioral suites above were not needlessly repeated afterward.

**Verification detail and limits.**

| Check | Observed result |
| --- | --- |
| `CUDA_VISIBLE_DEVICES='' python3 scripts/test_agent_board_resume.py` | 19 tests; exit 0 |
| `CUDA_VISIBLE_DEVICES='' python3 scripts/test_agent_board_serve.py` | 5 tests; exit 0 |
| `CUDA_VISIBLE_DEVICES='' python3 scripts/test_sync_slide_decks.py` | 17 tests; exit 0 |
| `env -u BASH_ENV -u ENV CUDA_VISIBLE_DEVICES='' python3 scripts/test_dispatch_scripts.py` | 12 tests; exit 0 |
| `bash -n scripts/deploy_cc.sh` | exit 0 |
| `bash -n scripts/snap_dispatch.sh` | exit 0 |
| `bash -n scripts/snap_health.sh` | exit 0 |
| `bash scripts/deploy_cc.sh --dry-run --name qa-probe --cwd /tmp --prompt-file /dev/null` | exit 2; regular-file requirement rejects `/dev/null` |
| Same preview with `/tmp/megaqa-A-qa-probe.md`, with default Claude or `--profile codex` | exit 0; exact arguments printed, checkpoint requested |
| `CUDA_VISIBLE_DEVICES='' python3 scripts/sync_slide_decks.py --check` | exit 0; this repository contains zero decks |
| `git diff --check ab715a0..HEAD` | exit 0 |
| `ls` of document/link targets | 45/45 present; exit 0 |

The snap launcher has no documented dry-run flag. Its run/tail/log/attach/kill behavior was exercised with local SSH/terminal mocks. Its unsupported `--help` form prints usage and exits **1**, which is not a failed remote-launch success. No paid cluster health probe or real remote deployment was needed for verification.

All six original board findings were checked: failed process discovery, duplicate prefix/full selection, current entrypoint classification, strict conversation identifier syntax, terminal-session identifiers rather than whitespace-split names, and nonzero partial-recovery failures. The first required the additional partial-output fix above. The original “23 tests” was 18 recovery tests plus five server tests; the corrected total is 19 plus five. The private board listener binds to `127.0.0.1`, serves only its intended files, rejects symlinks, and relies on private Tailscale Serve access rather than public Funnel. The [Tailscale Serve documentation](https://tailscale.com/docs/reference/tailscale-cli/serve) confirms that access controls still apply. Server regressions and responsive markup checks passed; actual account policy, phone/browser rendering, macOS service startup, and real model resume were not exercised on this Linux worker.

The three original slide false-pass categories in `a4939c0` were checked: source-plus-derivative hashes, staged-state validation, and unquoted filename/rename handling. Tests also cover missing or replaced derivatives and partial synchronization. New regression fixtures exercise converters that create output and fail, index read failures/type changes, final-deck deletion, hook reachability, and Keynote packages. Native LibreOffice/Keynote rendering was simulated; this review does not claim a real converter compatibility test or visual comparison of a deck.

The initial dispatch smoke-test fixture inherited `BASH_ENV`, which could resolve real subscription commands ahead of mocks; that attempt timed out, and token consumption was not established. No matching smoke process remained. The fixture now removes shell-startup overrides and verifies recorded mock arguments; subsequent tests passed. No direct provider application programming interface calls were authored or executed.

**Remaining observations, not in-scope critical/major blockers:**

18. **INFORMATIONAL — `workflows/remote-job-dispatch.md:197`, “Legacy agent selection is stale.”** The direct-mode workaround is verified; it does not repair the out-of-scope shared scheduler or `ssh-submit.sh` smart wrapper. [p 6/10] Repair those implementations in their own scope before recommending smart mode again, since their removed flags/unpinned models can prevent unattended jobs from starting.

19. **MINOR — `INDEX_RULES.md:116`, `INDEX_RULES.md:119`, `INDEX_RULES.md:124`, quoted `~/veribench/...` examples.** The shared VeriBench working checkout is old (`a11d74d57`, 2026-08-20). `ls` of the experiment-75 status, driver, task checkpoint, master checkpoint, and `~/veribench-wt-expt75` exited **2**. The four source files exist in already available Git objects: status/checkpoints on `origin/main`, driver and both prompt files on `origin/expt75-repair-driver` (`git cat-file -e` exited **0** for each). The dedicated worktree path is machine-specific. [p 5/10] Before using that example, create an isolated checkout of the cited branch; do not update or repurpose the shared working tree. Concrete repository script paths and both named token-file paths were verified with `ls` without reading token contents; the token directory is owner-only.

20. **MINOR — `INDEX_RULES.md:111` and `INDEX_RULES.md:130`, “Every dispatched worker…” / “A remote worker lands its own work…”.** Rules 42 and 46 remain long, dense paragraphs that bury parts of their action sequence. [p 3/10] Consider extracting the numbered Rule 46 procedure into a linked runbook while preserving its mandatory dispatch contract. This is a structural proposal only; no taste-driven rewrite was made. First-use explanations and duplicated/incorrect dispatch introduction text were improved.

The Stanford Network Analysis Project name was checked against the [official project site](https://snap.stanford.edu/); “Platform” names its software library. Model identifiers and effort settings were evaluated against the requested Hard Rule 8 contract and installed CLI help, without asserting account entitlement or revalidating historical release-date claims. New report timestamps came from `date`; quoted historical dates were not presented as fresh observations.

**TLDR-end:** [agents-config: quality assurance stage 1] The branch contains seventeen verified corrective commits and this handoff record, ready for stage 2. Operational limits and the remaining minor structural/out-of-scope observations are explicit; no push or merge was performed.
**Snapshot:**
```text
Recovery tests: 19 passed
Server tests:    5 passed
Slide tests:    17 passed
Dispatch tests: 12 passed
Total:         53 passed
Final corrective commit: cd37613
```
