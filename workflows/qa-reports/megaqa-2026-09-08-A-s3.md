# Mega QA (quality assurance) 2026-09-08 — strand A, stage 3

**TLDR-start:** [agents-config: quality assurance stage 3] Fresh review found additional false-success and false-failure paths in slide generation, worker discovery, recovery, dispatch, and landing instructions. Verified corrections are committed only on `megaqa-2026-09-08/A`; nothing is pushed or merged.

Timezone: Pacific Daylight Time (PDT).
Created: 2026-09-09 13:34 PDT
Last updated: 2026-09-09 13:38 PDT
Reviewer: Codex `gpt-6-astra`, reasoning `ultra`.
Scope: the 19 commits `3fcaa86^..ab715a0`, all 23 prior-stage commits through `3a02728`, and the resulting full in-scope files.
**Status:** DONE (verified corrective tip `0b48536`; 80 tests passed)

This report also records the recovery state: all corrective work is on the branch above; the final command is `git status --short`, with no push or merge. The explicitly protected shared checkout at `~/agents-config` was read without pulling or editing it. The current task's prohibition on shared-checkout writes overrides the generic refresh instruction.

## Verdict

```text
VERDICT: FIXED
CRITICAL_ISSUES: 0
MAJOR_ISSUES: 0
FIXES_APPLIED: 17
STRUCTURAL: IMPROVED
SUMMARY: Additional defects, including two critical slide freshness false passes, were corrected and verified locally. All 80 tests pass; no hosted checks are counted as evidence, and nothing is pushed or merged.
```

Counts describe remaining issues and this stage's corrective commits; the report commit is not a fix. Locations below refer to the corrected file; quotations identify the prior behavior where stated.

## Findings and corrections

1. **CRITICAL, fixed — `scripts/sync_slide_decks.py:227`, `5577e92`.** Previously, `"sha256": sha256_of(path)` could record a competing synchronizer's output against the wrong source. A deterministic two-process reproduction had both workers exit 0, the final source contain the newer content, the rendered Portable Document Format (PDF) file contain the older content, and the freshness check return no problems. A nonblocking advisory lock now serializes generation per worktree. The competing worker exits 3 before modifying the manifest. The contention regression fails against the preceding version, exit 1; the final slide suite passes, exit 0.

2. **CRITICAL, fixed — `scripts/sync_slide_decks.py:939`, `0c81b53`.** Previously, `digest = sha256_of(deck)` preceded rendering from the mutable original. Editing and restoring the source during rendering could certify output from the intervening content. Hashing, rendering, and text extraction now consume one private source copy. It sits beside the original to preserve relative media paths, retains the original display name in generated text, and is excluded from deck discovery. A lasting source change returns 3 and invalidates derivative records. Both regressions fail against the preceding version, exit 1; the final slide suite and compilation pass, exit 0.

3. **MAJOR, fixed — `scripts/sync_slide_decks.py:249`, `6e6c058`.** `ET.fromstring(...)` accepted well-formed but unsupported Extensible Markup Language (XML) roots, while declared missing/external speaker notes returned an empty string. These paths could certify an incomplete text dump. The parser now validates the expected roots and refuses unavailable declared notes. The negative regressions fail against the preceding version with five failing subcases, exit 1. Positive coverage preserves absolute slide ordering and relative notes; the final slide suite passes, exit 0.

4. **MAJOR, fixed — `scripts/agent_board.py:292`, `8192e78`.** The prior whole-command expression, including `|Claude Code`, treated prompt text or arguments such as `less /tmp/codex-pinned` as an agent. Matching now starts at the executable, with explicit support for packaged Node entrypoints. The regression rejects argument-only matches while retaining real pinned workers. Pinned cases fail against `2d927d5^`; argument-only cases fail against `2d927d5`, both exit 1. Final recovery tests and compilation pass, exit 0.

5. **MAJOR, fixed — `scripts/agent_board.py:1278`, `825af58`.** Remote pane parsing still used `exe not in ("codex", "claude")`, hiding the agent and model for healthy `claude-pinned`/`codex-pinned` workers. It now uses the executable-position classifier. Tests cover native copies, packaged commands, and unrelated commands; recovery tests and compilation pass, exit 0.

6. **MAJOR, fixed — `scripts/agent_board.py:786`, `2a621d3`; `machine/mac.md:64`.** Recovery previously created windows from one earlier process snapshot. Two calls during startup both exited 0 and opened two workers on the same transcript. Recovery now serializes each transcript, refreshes process and registry evidence under that lock, and records the exact host/server/window reservation through the startup gap. Unreadable registry entries and lost window-creation responses refuse another launch. Reservations are not expired by age; failed startup windows require inspection and explicit closure. Errors no longer suggest bypassing the refusal with a direct resume command. New overlap/startup/freshness/error tests pass; a real isolated tmux server with a harmless shell reproduced first recovery exit 0 and second recovery exit 1, then was removed. No model was started.

7. **MAJOR, fixed — `scripts/agent_board.py:301`, `acb0607`.** Anchoring alone left `(?:[-_]\S*)?` accepting executables such as `codex-helper` and `claude-monitor`. Native names are now explicitly limited to the agent name, `.exe`, or `-pinned`, consistent with deployment; packaged entrypoints remain supported. Four helper cases fail against `2a621d3`, exit 1, and pass after correction. Final recovery tests and compilation pass, exit 0.

8. **MAJOR, fixed — `scripts/agent_board.py:1108`, `0b48536`.** The collector correlated process startup with the original rollout's creation time, so a live `codex resume <thread-id>` could display as stopped. Explicit resume identifiers now identify the existing thread, while new threads retain creation-time matching and prompt text cannot masquerade as a resume subcommand. Service subcommands are also recognized only in command position: mentioning `app-server` or `sandbox` in a normal prompt no longer hides its live worker. Unspecified resume identity stays visibly uncertain. The collector regressions fail against `acb0607`, exit 1; the final 36-test recovery suite and compilation exit 0. Process command text does not preserve all argument boundaries, and creation-time matching for new threads remains a heuristic.

9. **MAJOR, fixed — `scripts/deploy_cc.sh:177`, `44354a8`.** The prior family-name suffix expression accepted unrelated helper executables and a different launch using the same runbook. Readiness now requires this launch's unique opening-prompt identifier and runbook path, supported executable identity, pane ancestry, non-zombie state, and stable process identity across two observations. Claude also requires a matching registry entry and process start time. Unrelated helper and same-basename reproductions returned 0 before and 1 after. Stage 2's original pinned-copy regression independently fails against `4af7377^`, exit 1. `bash -n scripts/deploy_cc.sh` and the final dispatch suite pass, exit 0.

10. **MAJOR, fixed — `scripts/deploy_cc.sh:126`, `148160e`.** The prior message said `still busy after 30s; typing anyway`. This could send the opening command into a startup program such as `kinit`. Deployment now exits 1 without typing and prints an inspection command. The accelerated startup regression fails before the fix, exit 1, and passes afterward, exit 0; the ready-shell case still passes. Syntax, regular-file previews, and the final dispatch suite pass, exit 0.

11. **MAJOR, fixed — `scripts/snap_dispatch.sh:95`, `ab7da5c`.** The prior `"${logdir}/${name}_${stamp}.log"` could be reused by two same-second launches after the first session was removed. `tee -a` then mixed both runs. A unique log is now reserved on the node and its actual path returned to the caller. The repeated-clock regression fails against the prior version, exit 1. Final dispatch tests pass, including duplicate refusal, remote launch failure exit 43, job failure status 7, argument preservation, and exact attach/kill targets. Syntax check exits 0.

12. **MAJOR, fixed — `scripts/snap_health.sh:419`, `de417be`.** Codex still probed `-m gpt-6-astra` while the output claimed flagship entitlement was not probed. The liveness probe now uses `gpt-5.6-terra` with medium reasoning, matching the regular-tier policy. Contrary to stage 2's report, that exact identifier was already present in `experiments/01_self_hosted_openclaw/standing_orders/experiment_dispatch.md:21` and its command example. The isolated mock verifies arguments without a model call. Syntax, help, the targeted smoke test, and the final dispatch suite exit 0. This verifies command construction, not account entitlement.

13. **MAJOR, fixed — `INDEX_RULES.md:130`, `d582cd9`.** The old sequence said `rebase ... re-run the deterministic checks` without preserving and comparing the original task content. An isolated repository reproduced an automatic conflict resolution that dropped a required correction while both rebase and Python compilation exited 0. The rule now preserves a local backup, reviews the old/new commit changes with `git range-diff`, and compares task-owned content with the saved commit. Every dropped or changed patch needs an explanation or repair; an empty range is not proof. The [Git range-diff documentation](https://git-scm.com/docs/git-range-diff) describes this as a review aid, not a machine-stable pass/fail format. The local content comparison detects the lost correction; fixture exit 0.

14. **MAJOR, fixed — `INDEX_RULES.md:130`, `e80c8a9`.** `Confirm gh pr view ... MERGED` and `Clean up ... per Rule 19` did not explain a merged request followed by failed local cleanup. The rule now checks merged state even after a nonzero merge-command result, omits combined branch deletion in worktree runs, separately deletes the remote branch with an expected-commit lease, verifies absence, and defers local cleanup while the worktree is active. The installed [GitHub command-line implementation, version 2.88.1](https://github.com/cli/cli/blob/v2.88.1/pkg/cmd/pr/merge/merge.go) performs the merge before local cleanup and remote deletion; local checkout failure can prevent the latter. Isolated verification: occupied-base checkout exit 128; absent remote reference query exit 2; guarded deletion dry run against a temporary bare fixture exit 0; incorrect expected commit rejected with exit 1. No network push occurred.

15. **MINOR, fixed — `INDEX_RULES.md:130`, `4805ff4`.** `uutils.emailing.send_email_smtp()` was named without proving the target interpreter could import it. The source file exists, but the default interpreter's import exited 1 because `dill` is missing. A runbook must now name and preflight its working interpreter. Failed delivery records a pending receipt and must not suppress the confirmed Git landing signal or become a false claim that mail was sent. No email was sent during this review; the shared Python environment was not modified. Documentation diff and path checks exit 0.

16. **MINOR, fixed — `INDEX_RULES.md:130`, `1b6d4ab`.** Review of this stage's own backup instruction caught that `record ... in the checkpoint` immediately before rebasing could dirty the tracked tree. The sequence now commits that record, then creates the backup branch before rebasing. It also defines the branch tip and merge base at first use. An isolated execution of the corrected checkpoint/backup/rebase/comparison sequence exits 0 and still exposes a deliberately dropped change; `git diff --check` exits 0.

17. **MINOR, fixed — `INDEX_RULES.md:111`, `:132`, `11c2742`.** The rule named a `private per-node copy` without the basename contract now required by the executable classifiers. It now names `claude-pinned` or `claude`, requires a worker-owned directory, and prohibits overwriting a copy in use. Rule 42 also describes the current launch identifier and two-observation readiness check. This keeps text and scripts consistent while preserving the intended private-copy workflow. Relevant files and the installed `claude-pinned` path exist; `ls` and documentation diff checks exit 0.

## Deterministic verification

All Python commands used `CUDA_VISIBLE_DEVICES=''`; test processes also removed `BASH_ENV` and `ENV` so startup configuration could not replace simulated commands with real subscription commands.

| Command | Result |
| --- | --- |
| `python3 scripts/test_agent_board_resume.py` | 36 tests, exit 0 |
| `python3 scripts/test_agent_board_serve.py` | 5 tests, exit 0 |
| `python3 scripts/test_dispatch_scripts.py` | 16 tests, exit 0 |
| `python3 scripts/test_sync_slide_decks.py` | 23 tests, exit 0 |
| `bash -n scripts/deploy_cc.sh scripts/snap_dispatch.sh scripts/snap_health.sh` | exit 0 |
| `python3 -m py_compile scripts/agent_board.py scripts/agent_board_phone.py scripts/agent_board_serve.py scripts/sync_slide_decks.py` | exit 0 |
| `bash scripts/deploy_cc.sh --dry-run --name qa-probe --cwd /tmp --prompt-file <temporary-regular-runbook> --profile cc` | exit 0; exact command and `CKPT_qa-probe.md` printed |
| Same dry run with `--profile ccv` and `--profile codex` | both exit 0 |
| User-suggested dry run with `--prompt-file /dev/null` | exit 2; runbook must be a regular file |
| `bash scripts/deploy_cc.sh --help` | exit 0 |
| `bash scripts/snap_dispatch.sh` | usage, exit 1; the driver's stated 0 was inaccurate |
| `bash scripts/snap_health.sh --help` | exit 0 |
| `git diff --check origin/main..HEAD`, `git diff --check 3a02728..HEAD`, working-tree check | all exit 0 |
| Parsed `claude-code-settings.json` model and effort assertions | pass, exit 0 |
| `ls` of local Markdown link targets in reviewed rule/settings/workflow docs | 42/42 exist, exit 0 |

The direct remote launcher has no documented dry-run option. Its transmitted shell program, locking, status handling, log paths, and `tail`/`log`/`attach`/`kill` arguments were exercised through local command simulations. The real isolated tmux check exercised recovery window/server identities with a harmless shell. Hosted automation did not run and is not evidence.

## Prior fixes and rule consistency re-checked

- All six original `8da03bf` recovery fixes remain covered: failed process discovery, duplicate prefix/full selection, latest entrypoint classification, strict conversation identifier syntax, exact tmux session identifiers, and nonzero partial failures. The earlier failed-process-scan correction still refuses partial output from a failed `ps`. This stage adds strict registry reads, startup serialization, and more precise agent identification.
- The original three slide false-pass categories remain covered: source/derivative hashes, Git's staged snapshot rather than the working tree, and unquoted filename/rename handling. Earlier corrections for failed renderer output, staged inspection failures, final-deck deletion, hook wiring, and Keynote directory packages remain intact. The new tests cover concurrency and changing source bytes.
- The private phone server binds to loopback and serves only its allowlisted board file; traversal and symlinks are refused by real request tests. The setup uses private Tailscale Serve, not public Funnel. Actual account access rules, macOS service startup, and phone layout rendering were not exercised on this Linux node.
- `AGENTS.md`, `CLAUDE.md`, and the settings template agree with Hard Rule 8's `gpt-6-astra`/ultra and `claude-fable-5-1`/max review defaults, regular experiment tier, recorded model identity, and subscription exhaustion policy. The remaining `xhigh` references in these rule mirrors explain allowed effort levels; they are not stale defaults. No active `gpt-5.6-sol` or old Fable identifier remains in the reviewed settings/handoff files.
- Trigger Rules 41–46 retain priority scoring, distinct worker/master checkpoint names, session ownership, live results, reference-data review by both model families, and the email routing of Rules 14/26. The earlier expansion of abbreviations did not alter those requirements. Rule 36 still keeps human-facing model recommendations outside the prompt and a closing summary inside reusable prompts.
- Local command help confirms `gh pr merge --match-head-commit`, the requested `gh pr view` fields, expected-commit leases, Claude effort/resume/fork/Remote Control flags, and Codex's full-permission execution flag. Installed tools: `gh 2.88.1`, `git 2.43.0`, `codex-cli 0.153.4`. Model policy was judged against the requested Hard Rule 8 and installed command syntax; this review does not assert model entitlement or validate historical release-date claims.
- Read-only protection queries returned `404 Branch not protected` for agents-config and `403 Upgrade to GitHub Pro...` for VeriBench, each command exit 1. These are configuration evidence, not successful hosted checks. Rule 46's explicitly disclosed interval in which unprotected `main` can advance remains a limitation of its mandated immediate-merge procedure.

## Remaining bounded observations

18. **MINOR, proposal — `INDEX_RULES.md:130`, “Merge immediately, and only what was verified”.** [p 4/10] The five-step Rule 46 procedure remains one very long paragraph, making required steps easy to miss; consider a linked mandatory runbook or mechanical step breaks in a later documentation pass. No taste-driven restructuring was done here. Script structure improved through small shared identity/publication helpers; no broad refactor or unmeasured complexity claim is made.

19. **INFORMATIONAL — `INDEX_RULES.md:116`, `:119`, `:124`, quoted `~/veribench/...` examples.** Four example files are absent from this node's old shared working checkout (`ls` exit 2), and the shortened prompt paths are relative to the cited driver directory, not this repository. All six full repository-relative files exist in already available `origin/main` or `origin/expt75-repair-driver` objects (`git cat-file -e` exit 0). The rule already instructs use of an owned worktree for the repair branch; no shared checkout was updated. These are historical examples, not evidence that those absolute working paths exist on this node.

20. **INFORMATIONAL — `workflows/remote-job-dispatch.md:197`, “Legacy agent selection is stale”.** The older scheduler and `ssh-submit.sh` smart wrappers remain outside this strand's implementation scope. The reviewed documentation correctly selects direct mode and explicitly names the worker model and completion protocol. No out-of-scope launcher was edited.

Renderer tests use simulated converters; there was no native LibreOffice/Keynote compatibility or visual-deck check. No real remote worker, paid health probe, provider application programming interface call, mail delivery, push, or merge was performed. Exact staged diffs were inspected and scanned before each commit.

**TLDR-end:** [agents-config: quality assurance stage 3] The corrected branch has no remaining identified in-scope critical or major finding. It is ready for the driver to apply the campaign's landing decision, with the local test evidence and remaining limitations recorded above.
**Snapshot:**
```text
Corrective commits this stage: 17
Recovery: 36 tests passed
Phone server: 5 tests passed
Dispatch: 16 tests passed
Slides: 23 tests passed
Total: 80 tests passed
No push or merge
```
